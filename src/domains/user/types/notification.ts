export interface NotificationType {
  id: number;
  title: string;
  createdAt: string;
}

export interface NotificationDetailType {
  id: number;
  title: string;
  content: string;
  createdAt: string;
}
