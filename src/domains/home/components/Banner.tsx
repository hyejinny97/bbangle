import PaddingWrapper from '@/components/commons/PaddingWrapper';

const Banner = () => (
    <PaddingWrapper className="pb-[0px]">
      <div className="w-full h-[164px] bg-blueGray-30 rounded-[10px]" />
    </PaddingWrapper>
  );

export default Banner;
