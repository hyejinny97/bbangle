// import { useQuery } from '@tanstack/react-query';
// import API from '@/api';
// import { IProductsType } from '@/components/units/Home/types';

// const getChatList = async (): Promise<IProductsType[]> => {
//     const result = await API.get<{ data: IProductsType[] }>('/chats');
//     return result.data;
// };

// export const useGetChatListQuery = () => {
//     return useQuery(['chatList-key'], getChatList);
// };
